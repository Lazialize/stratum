# Implementation Plan

## Tasks

- [x] 1. 名称ポリシーと設定/状態パスの統一
- [x] 1.1 設定ファイルの既定パスを`strata`に統一する
  - 新規初期化時の出力先が`.strata.yaml`になることを保証する
  - 既存の読み取り/書き込み経路が旧名称パスに書き込まないようにする
  - 設定パスの参照が単一の名称ポリシーに集約されるようにする
  - _Requirements: 2.1, 2.3_

- [x] 1.2 状態ディレクトリの既定パスを`strata`に統一する
  - 実行時の状態/ログ保存先が`.strata/`になることを保証する
  - 旧名称ディレクトリへの新規出力が発生しないようにする
  - 状態パスの参照が単一の名称ポリシーに集約されるようにする
  - _Requirements: 2.2, 2.3_

- [x] 2. CLIコマンド名と表示名の更新
- [x] 2.1 CLIの実行名とヘルプ表記を`strata`に統一する
  - 実行名が`strata`で主要コマンドを同等に利用できる状態にする
  - ヘルプ/バージョン表示のアプリ名を`strata`にする
  - 端末出力や利用案内に`strata`表記を適用する
  - _Requirements: 1.1, 1.2, 3.1_

- [x] 3. (P) パッケージ/バイナリ名の更新
- [x] 3.1 (P) 配布物の識別子を`strata`に統一する
  - ビルド成果物の実行ファイル名が`strata`になるようにする
  - パッケージ識別子が`strata`として公開されるようにする
  - _Requirements: 4.1, 4.2_

- [x] 4. (P) ドキュメント/サンプル表記の更新
- [x] 4.1 (P) ユーザー参照資料の表記を`strata`に統一する
  - README/BUILDING/CONTRIBUTING等の利用手順を`strata`表記に更新する
  - サンプルやガイド内のコマンド例を`strata`で統一する
  - 配布/インストール手順の前提を`strata`に合わせる
  - _Requirements: 3.2, 4.3_

- [x] 5. テスト更新と回帰確認
- [x] 5.1 CLIと設定パスの変更に対応したテストを更新する
  - `strata`実行名で主要コマンドが利用可能であることを検証する
  - `.strata.yaml`/`.strata/`の生成・参照が正しいことを検証する
  - 旧名称パスに新規出力が行われないことを検証する
  - _Requirements: 1.1, 1.2, 2.1, 2.2, 2.3_
